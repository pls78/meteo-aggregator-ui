# Tasks — weather-panel-feedback

## 1. Implementation

- [x] 1.1 Rewrite the "backend running" error copy in `LocationCard`, `WeatherSheet`, `HourlyPanel`
- [x] 1.2 `WeatherSheet`: scroll the day-detail section into view when a day/view is selected (reduced-motion aware)

## 2. Verify and finish

- [x] 2.1 Build + lint; verify the scroll behavior in the browser; detector over changed targets
- [x] 2.2 Sync `weather-display` spec; retire the DESIGN.md error-copy deviation; archive
