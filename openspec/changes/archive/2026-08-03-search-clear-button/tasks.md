# Tasks — search-clear-button

## 1. Implementation

- [x] 1.1 Add `ClearIcon` (filled circle ✕) to `components/icons.tsx`
- [x] 1.2 `SearchBox`: move focus tracking to the container (relatedTarget check); render the clear control when text + focus-within; clear keeps focus and selection

## 2. Verify and finish

- [x] 2.1 Build + lint pass; screenshot round (desktop editing state, comparison bar mid-edit, mobile); detector over changed targets
- [x] 2.2 Sync `location-search` spec; archive
