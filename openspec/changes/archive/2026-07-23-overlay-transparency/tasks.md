> Backfill of shipped work — all tasks are already implemented, deployed, and verified.

## 1. Overlays

- [x] 1.1 Set the floating overlay panels/controls to `bg-white/70` + `backdrop-blur` in both layouts: search box + add button, location card, layer control, time-lapse control, about/locate buttons, mobile top bar, weather sheet, layers sheet.
- [x] 1.2 Leave dense reading surfaces opaque: the about dialog and dropdown result lists.

## 2. Verify

- [x] 2.1 Map shows through the overlays and their content stays legible over the map.
